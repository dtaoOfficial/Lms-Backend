
###############################################
# ? BASIC APP CONFIG
###############################################
spring.application.name=${SPRING_APPLICATION_NAME:lms}
server.port=${PORT:8080}
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://localhost:3000}


###############################################
# ? DATABASE (MongoDB)
###############################################
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://mahesh:Mahesh4613@learning.f1gtebq.mongodb.net/lms_db?retryWrites=true&w=majority&tls=true&appName=Learning}

###############################################
# ? JWT SECURITY SETTINGS
###############################################
jwt.secret=${JWT_SECRET:super_long_random_secret_here_at_least_32_bytes_please_change_me}
jwt.expiration.seconds=${JWT_EXPIRATION_SECONDS:900}
jwt.rememberme.seconds=${JWT_REMEMBER_ME_SECONDS:1209600}
jwt.issuer=${JWT_ISSUER:lms-api}

###############################################
# ? MAIL CONFIG (Brevo API ? no SMTP)
###############################################
# Brevo API key is stored in .env
brevo.api.key=${BREVO_API_KEY}

# Application mail identity
app.mail.from=${APP_MAIL_FROM:no-reply@dtaoofficial.in}
app.mail.name=${APP_MAIL_NAME:DTAO LMS}
app.mail.owner=${APP_MAIL_OWNER:DTAO OFFICIAL}
app.mail.company=${APP_MAIL_COMPANY:DTAO OFFICIAL}
app.mail.website=${APP_MAIL_WEBSITE:https://dtaoofficial.netlify.app}

###############################################
# ? LOGGING CONFIG
###############################################
logging.level.org.springframework.security=${LOGGING_LEVEL_ORG_SPRINGFRAMEWORK_SECURITY:INFO}
logging.level.org.springframework.web=${LOGGING_LEVEL_ORG_SPRINGFRAMEWORK_WEB:INFO}
logging.level.org.mongodb.driver=${LOGGING_LEVEL_ORG_MONGODB_DRIVER:INFO}

# Custom app logging
logging.level.com.dtao.lms.service=DEBUG
logging.level.com.dtao.lms.controller=DEBUG
logging.level.org.springframework.data.mongodb.core=DEBUG
logging.level.org.springframework.data.mongodb.repository=DEBUG
logging.level.com.dtao.lms.service.ProgressService=DEBUG
logging.level.com.dtao.lms.controller.ProgressController=DEBUG

###############################################
# ? LOAD ENV FILE
###############################################
spring.config.import=optional:file:.env[.properties]

###############################################
# ? UPLOAD SETTINGS
###############################################
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

###############################################
# ?? SYSTEM SETTINGS DEFAULTS
###############################################
system.autoApproveCourses=true
system.maintenanceMode=false
system.allowRegistration=true
system.defaultRole=STUDENT
system.themeMode=light

spring.profiles.active=${SPRING_PROFILES_ACTIVE:prod}
spring.jackson.time-zone=${TZ:Asia/Kolkata}

